const $=s=>document.querySelector(s);const state={items:JSON.parse(localStorage.getItem("carStealItems")||"[]"),view:"all"};const fmt=n=>n==null?"Price ?":new Intl.NumberFormat("en-CA",{style:"currency",currency:"CAD",maximumFractionDigits:0}).format(n);const esc=s=>String(s||"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]));function source(url=""){try{const h=new URL(url).hostname;if(h.includes("facebook"))return"Facebook Marketplace";if(h.includes("kijiji"))return"Kijiji";if(h.includes("autotrader"))return"AutoTrader";return h}catch{return"Manual"}}function persist(){localStorage.setItem("carStealItems",JSON.stringify(state.items))}function saveListing(url,text){const scored=CarScore.score({id:url||crypto.randomUUID(),url,source:source(url),title:text.split("\n").find(x=>x.trim())||"Vehicle listing",text,capturedAt:new Date().toISOString()});const idx=state.items.findIndex(x=>(url&&x.url===url)||x.id===scored.id);if(idx>=0)state.items[idx]={...state.items[idx],...scored,lastSeen:new Date().toISOString()};else state.items.unshift({...scored,firstSeen:new Date().toISOString(),lastSeen:new Date().toISOString()});persist();render();return scored}function filters(){return{maxPrice:Number($("#maxPrice").value),maxKm:Number($("#maxKm").value),safety:$("#safetyOnly").checked}}function isPriority(x){return/toyota|honda|pontiac\s+vibe|scion/i.test(`${x.title} ${x.text}`)}function render(){const f=filters();let xs=state.items.filter(x=>(x.price??Infinity)<=f.maxPrice&&(x.km??0)<=f.maxKm&&(!f.safety||x.safetyConfirmed));if(state.view==="steals")xs=xs.filter(x=>x.score>=85);if(state.view==="priority")xs=xs.filter(isPriority);const sort=$("#sort").value;if(sort==="score")xs.sort((a,b)=>b.score-a.score||(a.price||Infinity)-(b.price||Infinity));if(sort==="newest")xs.sort((a,b)=>new Date(b.lastSeen)-new Date(a.lastSeen));if(sort==="price")xs.sort((a,b)=>(a.price||Infinity)-(b.price||Infinity));if(sort==="km")xs.sort((a,b)=>(a.km||Infinity)-(b.km||Infinity));$("#steals").textContent=state.items.filter(x=>x.score>=85).length;$("#matches").textContent=xs.length;$("#saved").textContent=state.items.length;if(!xs.length){$("#feed").innerHTML='<div class="empty"><b>No matching cars yet.</b><br><br>Tap Add or Paste from Clipboard when you find a Marketplace/Kijiji listing.</div>';return}$("#feed").innerHTML=xs.map(x=>`<a class="card" href="${esc(x.url||"#")}" ${x.url?'target="_blank"':''}><div class="top"><div class="title">${esc(x.title)}</div><div class="badge ${(x.tier||"").toLowerCase()}">${x.tier} ${x.score}/100</div></div><div class="price">${fmt(x.price)}</div><div class="meta">${x.year||"Year ?"} • ${x.km?x.km.toLocaleString()+" km":"km ?"} • ${x.safetyConfirmed?"✓ safety":"safety ?"}</div><div class="reason">${esc((x.reasons||[]).slice(0,5).join(" · "))}</div><div class="url">${esc(x.source||"")}${x.url?" • "+esc(x.url):""}</div></a>`).join("")}function openAdd(u="",t=""){ $("#url").value=u;$("#text").value=t;$("#modal").classList.remove("hidden");setTimeout(()=>$("#text").focus(),100)}$("#addBtn").onclick=()=>openAdd();$("#closeBtn").onclick=()=>$("#modal").classList.add("hidden");$("#saveBtn").onclick=()=>{const u=$("#url").value.trim(),t=$("#text").value.trim();if(!u&&!t)return;saveListing(u,t||u);$("#modal").classList.add("hidden")};$("#pasteBtn").onclick=async()=>{try{const t=await navigator.clipboard.readText();const url=(t.match(/https?:\/\/\S+/)||[])[0]||"";openAdd(url,t)}catch{openAdd()}};$("#helpBtn").onclick=()=>$("#help").classList.remove("hidden");$("#helpClose").onclick=$("#helpDone").onclick=()=>$("#help").classList.add("hidden");["maxPrice","maxKm","safetyOnly","sort"].forEach(id=>$("#"+id).addEventListener("change",render));document.querySelectorAll("nav button").forEach(b=>b.onclick=()=>{document.querySelectorAll("nav button").forEach(x=>x.classList.remove("active"));b.classList.add("active");state.view=b.dataset.view;render()});const q=new URLSearchParams(location.search);if(q.get("share")){openAdd(q.get("share"),"");history.replaceState({},"",location.pathname)}if("serviceWorker"in navigator)navigator.serviceWorker.register("./sw.js");render();